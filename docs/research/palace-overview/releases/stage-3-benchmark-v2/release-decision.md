# Palace Overview Stage 3 — doctrine completion + Benchmark V2

Baseline parent: check `git merge-base` at merge time. Observed while writing: `a326ebf`.

## Decision

```json
{
  "research": "READY_FOR_EXPERT_DATA_COLLECTION",
  "calibration": "NO_GO",
  "shadow": "NO_GO",
  "production": "NO_GO"
}
```

This is **not** a release. Numeric scores remain frozen.

## Next

PALACE OVERVIEW CALIBRATION — STAGE A (quality normalization only) after sufficient human Benchmark V2 data. Freeze geometry, brightness, star axes, Tứ Hóa, minor families, structural interactions, VCD numbers.
