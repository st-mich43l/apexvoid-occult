# Palace Overview knowledge V2

Nam Phái radar scoring seeds from teacher proposal 2026-08-14.

- Coefficients live in `formula.json`.
- Provenance lives in `sources.json`.
- V1 knowledge under `../v1/` is **not deleted** (rollback / Trung Châu / freeze tests).
- Formation multiplier `K` is **off** until teacher sets a value for in-repo systems only.
- Do not add cách cục catalogs from memory.
