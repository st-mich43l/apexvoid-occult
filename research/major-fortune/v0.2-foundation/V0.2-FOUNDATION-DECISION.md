# Major Fortune V0.2 Foundation — Decision

Base master SHA: `e57cf2c87228e4adc41f211ce16c8c8e6d10f4e2`

## Foundation decision

**`RESEARCH_INCOMPLETE`**

Not production-ready. Not a candidate freeze. V0.1 remains the only experimental MF scorer API; production routing stays `rebuilding`.

## Integrity vs doctrine

| Layer | Status |
| --- | --- |
| Validation / audit integrity | Passed (all-cycle corpus, clip gates, AJV schemas, frozen V0.1 control, scoreState, pillar→module status, star-pattern pairwise context) |
| Research doctrine | Still incomplete — no sourced executable `rawDelta` |
| Executable numeric contributions | None (`executableContributionRuleIds: []`) |
| Production approval | Denied |
| V0.1 frozen control | Unchanged vs `master@e57cf2c` fixture |
| Production Major Fortune routing | Remains `rebuilding` |

## Why incomplete

1. Intake is an unverified internal research brief — no classical page locators for rawDelta magnitudes.
2. Per-rule `rawDelta` values are intentionally `null` / non-contributing (`executableContributionRuleIds: []`).
3. Nam Phái Major Fortune transformations are **blocked-by-calculation-core**.
4. Tuần/Triệt thresholds are research-blocked.
5. Annual Thái Tuế path blocked; natal Thái Tuế ring unproven.
6. Tam Không exception blocked without atomic policy.

## Formula (candidate)

```
pillarRaw = sum(matched executable rule rawDelta)
pillarDelta = clamp(pillarRaw, -cap, +cap)   // raw may exceed cap; clip once
preClampScore = 50 + sum(pillarDelta)
score = clamp(preClampScore, 0, 100)  // 1 decimal
```

Caps: Thiên Thời 30 · Địa Lợi 25 · Nhân Hòa 20 · Tứ Hóa & Sát Tinh 25.

Hard clip gate: `abs(cappedDelta) > cap + ε` fails; `pillarRaw > cap` is a clip event only.

`scoreState` uses `netCappedDelta` + `signalMass` (not `sum(abs(pillarRaw))`).

## Audit integrity (corrective)

- Corpus: 100 unique birth charts; train 80 / holdout 20 at **chart** level; each chart expanded across every Core-reachable Major Fortune cycle (`annualYear ∈ [1900,2100]`); no chart leaks across splits; product fixtures excluded from calibration.
- Metrics report both `chartCount` and `cycleObservationCount`.
- V0.1: `v01Deterministic` (same-branch rerun) vs `v01FrozenControlEquivalent` (frozen fixture from `e57cf2c`); production-safety hard gate uses frozen control.
- Schemas: AJV validates registries, policies, contradiction log, contracts, and runtime V0.2 knowledge; CLI exits non-zero on failure.
- Star-pattern structural matches use natal Mệnh ↔ active MF palace pairwise compatibility only; no invented numeric deltas.

## Audit snapshot (seed 20260723)

| Metric | Value |
| --- | --- |
| Charts / cycle observations | 100 / 2332 |
| Train / holdout charts | 80 / 20 (no chart leak) |
| `includeAllAvailableMajorFortuneCycles` | true (Core-reachable; `annualYear ∈ [1900,2100]`) |
| Score min/max/mean | 50 / 50 / 50 |
| Band | 100% `binh-on` |
| Score state | 100% `partial-data` |
| Executable contributions | 0 |
| V0.1 deterministic / frozen control | true / true |
| Hard gate failures | none |

Research gates only — distribution collapse at 50 is expected until sourced rawDelta values exist. **Not** a production approval.

## Calculation Core blockers

| Blocker | School | Next task |
| --- | --- | --- |
| MF stem + Tứ Hóa producers | nam-phai | Separate Core PR; do not fabricate in scorer |
| Void numeric thresholds | both | Source-backed threshold table |
| Natal Thái Tuế ring proof | both | Prove independence from annualYear |

## Source / claim counts

- Sources: 3 (`SRC-MF-V02-INTAKE-001`, `SRC-MF-V02-CAP-001`, `SRC-MF-V02-CORE-BRANCH-001`)
- Claims: 14 (`CLM-MFV02-001` … `014`)
- Policies: 9
- Rules: 58 (all non-executable / blocked)

## Production routing

`getAnalysisStatus("major-fortune")` unchanged (`unavailable` / `rebuilding`).
No UI. No feature flag.
